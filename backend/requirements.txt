# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2

# ML / VLM
torch==2.4.1
torchvision==0.19.1
transformers==4.44.2
accelerate==0.34.2
peft==0.13.0
bitsandbytes==0.43.3
huggingface_hub==0.25.1

# Segmentation (install SAM 2 separately from Meta's GitHub repo — see models/README.md)
opencv-python==4.10.0.84
pillow==10.4.0
numpy==1.26.4

# Geospatial processing
rasterio==1.3.11
# GDAL is easiest installed via conda (conda install -c conda-forge gdal)
# or system package (apt install gdal-bin libgdal-dev) — pip install can be finicky
torchgeo==0.6.0

# Vector DB (pick one)
qdrant-client==1.11.2
# psycopg2-binary==2.9.9   # uncomment if using PostGIS + pgvector instead
